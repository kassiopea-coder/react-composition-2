/**
 * Компонент "Заголовок"
 * Принимает props:
 * - текст заголовка
 * - ссылка с заголовка
 * - является ли элемент активным (подсвеченным)
 */
export default function Title({ text, link, selected }) {
  return (
    <h2>
    </h2>
  );
}