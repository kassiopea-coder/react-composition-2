/**
 * Компонент "Эфир"
 * Обеспечивает вывод элемента списка Эфиров
 * Принимает props:
 * - телепрограмма
 * - название эфирного канала
 * - ссылка на страницу вещания
 */
export default function EfirItem({ telecast, channel, link }) {
  return (
    <li>
    </li>
  );
}