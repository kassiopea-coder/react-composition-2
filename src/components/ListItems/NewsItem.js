/**
 * Компонент "Новости"
 * Обеспечивает вывод элемента списка Новостей
 * Принимает props:
 * - логотип СМИ
 * - заголовок новости
 * - ссылка на новость
 */
export default function NewsItem({ logo, subject, link }) {
  return (
    <li>
    </li>
  );
}