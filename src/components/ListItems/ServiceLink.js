/**
 * Компонент "Внутренний Сервис"
 * Обеспечивает вывод элемента списка Внутренних Сервисов
 * Принимает props:
 * - название сервиса
 * - ссылка на сервис
 */
export default function ServiceLink({ title, link }) {
  return (
    <li>
    </li>
  );
}