/**
 * Компонент "Посещаемое"
 * Обеспечивает вывод элемента списка Посещаемых Сервисов
 * Принимает props:
 * - название сервиса
 * - краткое описание
 * - ссылка на сервис
 */
export default function FavoritesItem({ title, description, link }) {
  return (
    <li>
    </li>
  );
}