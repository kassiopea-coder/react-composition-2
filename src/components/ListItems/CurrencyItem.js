/**
 * Компонент "Курс Валюты"
 * Обеспечивает вывод элемента списка Курсов Валют
 * Принимает props:
 * - название валюты
 * - текущий курс
 * - разница курсов с предыдущим значением
 * - ссылка на страницу графика курса валюты
 */
export default function CurrencyItem({ currency, cost, diff, link }) {
  return (
    <li>
    </li>
  );
}