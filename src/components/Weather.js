/**
 * Компонент "Погода"
 * Принимает props:
 * - иконка текущей погоды
 * - объект с текущей, дневной и ночной температурой
 */
export default function Weather({ icon, temperature }) {
  return (
    <div>
    </div>
  );
}